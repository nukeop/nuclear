describe('Pagination', () => {
  it.todo('(Snapshot) renders a middle page with ellipses on both sides');
  it.todo('(Snapshot) renders the first page with previous disabled');
  it.todo('(Snapshot) renders the last page with next disabled');
  it.todo('calls onPageChange with the page number when a page is clicked');
  it.todo('moves one page back and forward via previous and next');
  it.todo('renders nothing when there is only one page');
});
