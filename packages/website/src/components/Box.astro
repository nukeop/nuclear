---
type Props = {
  as?: string;
  class?: string;
  [key: string]: unknown;
};

const { as: Element = 'div', class: className, ...rest } = Astro.props;
---

<Element
  class:list={[
    'themed-border border-border bg-primary shadow-shadow relative overflow-hidden rounded-md text-center',
    className,
  ]}
  {...rest}
>
  <slot name="badge" />
  <slot />
</Element>
