os: linux
language: node_js
node_js:
- 12

before_script:
 NODE_OPTIONS=--max_old_space_size=8192
 travis_wait 30 npm test

deploy:
 provider: pages
 github-token: $GITHUB-TOKEN
 on: 
   branch: master